#include <stdio.h> 

void bubble_sort_recursion(char nama[], int N){
    for ( int i = 0; i < N - 1; i++ ){
        int swapped = 0; 
        for ( int j = 0; j < N - i - 1; j++ ){
            if ( )
        }
    }
}
int main(){
    int N; 
    scanf("%d", &N); 

    for ( int tc = 0; tc < N; tc++ ){
        char nama[1005]; 
        int J, P; 
        scanf("%s %d %d", nama, &J, &P); 

        int result = J * P; 
        bubble_sort_recursion(nama, 0, N - 1); 
    }
}