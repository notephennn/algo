#include <stdio.h> 

int main()
    {
        double A, B, C, D, sum; 
        scanf("%lf %lf %lf %lf", &A, &B, &C, &D, sum); 
        sum = 2*A + 4*B/2 + 6*C/3 + D; 
        printf("%.2lf\n", sum);
        scanf("%lf %lf %lf %lf", &A, &B, &C, &D, sum); 
        sum = 2*A + 4*B/2 + 6*C/3 + D; 
        printf("%.2lf\n", sum);
        scanf("%lf %lf %lf %lf", &A, &B, &C, &D, sum); 
        sum = 2*A + 4*B/2 + 6*C/3 + D; 
        printf("%.2lf\n", sum);
    }